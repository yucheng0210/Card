using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;

public class MoveEffect : IEffect
{
    public void ApplyEffect(int value, int target)
    {
        //BattleManager.Instance.CheckerboardTrans.GetComponent<Button>().onClick.AddListener(()=>Move(,value));
    }
    private void Move(Transform moveTrans, int value)
    {
        //BattleManager.Instance.PlayerTrans.SetParent();
    }
}
